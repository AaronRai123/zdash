# Z-DASH JSON Schema Changelog

## 1.0.0 - 2026-02-04

- Initial stable schema release.
- Covers `check`, `scan`, `stats`, `repair`, and `sample` JSON success/failure payloads.
- Contract documented in `JSON_SCHEMA.md`.
